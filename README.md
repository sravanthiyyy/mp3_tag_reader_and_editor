# mp3
 reads and  manages ID3 metadata from MP3 files through the command line. The  application processes audio files and displays details such as title, artist,  album, genre, and year in a structured format. It also supports modifying  and updating tag information efficiently using file handling techniques.
